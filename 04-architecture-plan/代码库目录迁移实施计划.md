# 代码库目录迁移实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 将正式后端、正式前端和基础设施迁移到根级 `codebase/`，保持 Stage 3 原型原位，并把该目录约束同步到正式软件交付工作流技能。

**架构：** 阶段交付物继续由 `00`—`08` 编号目录管理，跨阶段治理材料继续由 `workflow/` 管理，长期演进的工程文件统一由 `codebase/` 管理。迁移只改变目录和有效路径引用，不改变产品行为、依赖、API、数据模型或原型内容。

**技术栈：** Git、Markdown、Python/FastAPI、Node.js 静态检查、Docker Compose 配置。

## 全局约束

- `03-ui-prototype/prototype/` 必须保持原路径、原内容，且是 Stage 3 原型唯一事实来源。
- 不创建 `codebase/prototype/`，不复制原型代码。
- 正式代码不得放入 `05-development/`。
- 不保留 `backend/`、`frontend/`、`infra/` 旧路径兼容副本。
- 不修改真实密钥、生产 `.env`、容器卷或无关文件。
- 不新增生产依赖、兼容代码或抽象层。
- 历史台账中绑定旧 Commit 的路径和命令保持原样；只更新当前有效基线和后续执行入口。

---

### Task 1：建立统一代码库目录并迁移工程文件

**Files:**
- Create: `codebase/`
- Move: `backend/` → `codebase/backend/`
- Move: `frontend/` → `codebase/frontend/`
- Move: `infra/` → `codebase/infra/`
- Preserve: `03-ui-prototype/prototype/`

**Interfaces:**
- Consumes: 当前根级正式工程目录。
- Produces: `codebase/backend/`、`codebase/frontend/`、`codebase/infra/` 三个稳定工程入口。

- [x] **Step 1: 记录迁移前目录与 Git 状态**

Run: `git status --short && find backend frontend infra -maxdepth 2 -type f -print`

Expected: 三个源目录存在；用户已有未跟踪文件仅记录，不纳入迁移提交。

- [x] **Step 2: 使用 Git 重命名迁移目录**

Run: `mkdir -p codebase && git mv backend codebase/backend && git mv frontend codebase/frontend && git mv infra codebase/infra`

Expected: Git 将已有文件识别为重命名，根目录不再存在三个旧目录。

- [x] **Step 3: 验证原型未被移动或复制**

Run: `test -d 03-ui-prototype/prototype && test ! -e codebase/prototype && test ! -e backend && test ! -e frontend && test ! -e infra`

Expected: 命令退出码为 0。

### Task 2：更新项目当前有效路径基线

**Files:**
- Modify: `04-architecture-plan/AGENTS.md`
- Modify: `04-architecture-plan/IMPLEMENTATION_PLAN.md`
- Modify: `04-architecture-plan/代码库目录归档设计.md`
- Modify: `05-development/TEAM_WORK_PACKAGES.md`
- Modify: `05-development/DEV_NOTES.md`
- Modify: `05-development/SELF_TEST.md`
- Modify: `workflow/CHANGE_REQUESTS.md`

**Interfaces:**
- Consumes: Task 1 产生的 `codebase/` 路径。
- Produces: 后续 Stage 5 开发、验证和交付使用的唯一有效工程路径。

- [x] **Step 1: 更新 Stage 4 开发约束和实施计划**

将当前有效的 `backend/`、`frontend/`、`infra/` 引用分别替换为 `codebase/backend/`、`codebase/frontend/`、`codebase/infra/`；Stage 3 原型和 Stage 6 原型静态检查路径不变。

- [x] **Step 2: 更新 Stage 5 当前执行入口**

更新团队工作包中的文件所有权和冻结接口；在开发记录与自测文档追加本次目录迁移后的当前命令，不改写历史检查点和历史命令证据。

- [x] **Step 3: 更新设计与变更状态**

将设计状态更新为已实施待验证；将 CR-032 更新为相同状态，并记录无兼容副本、无原型迁移和无产品行为变化。

- [x] **Step 4: 扫描失效入口**

Run: `rg -n '(^|[^[:alnum:]_])(backend/|frontend/|infra/)' README.md 04-architecture-plan 05-development 08-release-handoff --glob '*.md'`

Expected: 当前有效说明不再使用旧路径；允许保留明确标注为历史证据的旧 Commit 路径。

### Task 3：同步正式软件交付工作流技能约束

**Files:**
- Modify: `/Users/qiqi/.codex/skills/formal-software-delivery-workflow/SKILL.md`
- Modify: `/Users/qiqi/.codex/skills/formal-software-delivery-workflow/references/artifacts.md`

**Interfaces:**
- Consumes: 已确认的 `04-architecture-plan/代码库目录归档设计.md`。
- Produces: 后续项目可复用的阶段工件、代码库和原型归档约束。

- [x] **Step 1: 在主技能中增加工程代码库归档规则**

新增强制规则：正式源代码、基础设施和工程测试可统一使用根级 `codebase/`；不得放入 `05-development/`；Stage 3 原型必须保留在 `03-ui-prototype/`，不得复制到代码库。

- [x] **Step 2: 更新工件目录参考**

在默认布局和当前有效工作区映射中加入 `codebase/backend/`、`codebase/frontend/`、`codebase/infra/`、可选 `codebase/tests/`，并明确服务测试跟随服务、Stage 6 保存测试文档和证据。

- [x] **Step 3: 校验技能文字一致性**

Run: `rg -n 'codebase|03-ui-prototype|05-development|Product source code|Automated test code' /Users/qiqi/.codex/skills/formal-software-delivery-workflow/SKILL.md /Users/qiqi/.codex/skills/formal-software-delivery-workflow/references/artifacts.md`

Expected: 主技能与参考文件对代码、原型、测试和阶段文档的归档边界一致。

### Task 4：运行迁移验证并记录结果

**Files:**
- Modify: `05-development/SELF_TEST.md`
- Modify: `workflow/CHANGE_REQUESTS.md`

**Interfaces:**
- Consumes: Tasks 1—3 的目录、文档与技能变更。
- Produces: 可审计的迁移验证结果和精确 Git 提交。

- [x] **Step 1: 运行后端最小测试**

Run: `pytest codebase/backend/tests/test_health.py -q`

Expected: 4 tests passed；若环境缺少依赖，使用项目既有容器命令，仍不可虚构结果。

- [x] **Step 2: 运行 Stage 3 原型静态回归**

Run: `for f in 06-testing/tests/*.test.js; do node "$f"; done`

Expected: 所有脚本退出码为 0。

- [x] **Step 3: 校验 Compose 配置**

Run: `docker compose --env-file codebase/infra/.env.example -f codebase/infra/docker-compose.yml config --quiet`

Expected: 退出码为 0；若执行环境没有 Docker，记录为未验证及真实原因。

- [x] **Step 4: 运行目录和差异检查**

Run: `test -d codebase/backend && test -d codebase/frontend && test -d codebase/infra && test -d 03-ui-prototype/prototype && test ! -e codebase/prototype && git diff --check`

Expected: 命令退出码为 0。

- [x] **Step 5: 检查完整 diff 并提交**

只暂存本计划范围内的代码库迁移、有效路径文档和 CR-032；不得暂存用户已有的 `* 2.md` 未跟踪文件。

Commit: `chore(repo): adopt unified codebase layout`

- [x] **Step 6: 回填精确提交并推送分支**

将迁移 Commit SHA 写入 CR-032 与开发记录，形成独立文档提交后推送 `codex/codebase-layout`。远程分支必须包含目录迁移和可审计验证记录。
