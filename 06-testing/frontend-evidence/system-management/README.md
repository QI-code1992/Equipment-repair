# 系统管理原型还原证据

## 对照清单

| 标签页 | 原型范围 | 正式数据与可写边界 | 本轮验收项 |
| --- | --- | --- | --- |
| 角色管理 | 搜索、提示标签、角色表六列、状态、分页、新增与编辑权限弹窗 | `GET /api/roles`、`GET /api/users`、`GET /api/permissions`；仅编辑已有角色权限调用 `PATCH /api/roles/{id}/permissions` | 原型工具栏、表格、分页、弹窗、权限树滚动 |
| 用户管理 | 左侧组织树、搜索、角色筛选、七列表格、分页、新增/编辑/详情弹窗 | `GET /api/users`、`POST /api/users`、`PATCH /api/users/{id}`、`GET /api/roles`；无用户组织关联、姓名、电话、邮箱、重置、删除 API | 七列结构、缺失字段提示、真实创建/启停、弹窗与分页 |
| 登录日志 | 搜索、结果与时间筛选、七列表格、结果标签、刷新、分页 | `GET /api/audit-events`；无姓名、来源 IP、终端和说明字段 | 原型筛选与列完整，字段缺口受控，不伪造数据 |
| 操作日志 | 搜索、模块与结果筛选、七列表格、结果标签、刷新、分页 | `GET /api/audit-events`；无操作人名称和审计说明字段 | 原型筛选与列完整，字段缺口受控，不伪造数据 |

## 当前证据状态

- 代码结构和自动化回归已完成；不等同于视觉验收。
- 固定视口：`1440 x 900`，浏览器缩放：100%。
- 原型来源：`03-ui-prototype/prototype/pages/system-management.html`。
- 正式路由：`/system-management`。
- 截图状态：待补充。2026-08-13 尝试在浏览器中打开原型时，浏览器安全策略拒绝 `file://` 地址；正式路由还要求有效认证会话和可用正式数据。该阻断已记录，未使用 mock 数据或自动化测试替代截图。

后续人工视觉验收必须在同一数据和权限状态下，分别保存角色管理、用户管理、登录日志和操作日志的原型/正式页面截图，并记录候选 Commit SHA、时间和操作者。
