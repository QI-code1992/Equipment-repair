# 项目文档与原型交接版本说明

## 版本信息

- 版本标签：`handoff/candidate-v20260714-01`
- 版本性质：候选交接快照，不是生产发布版本
- GitHub 仓库：https://github.com/QI-code1992/Equipment-repair
- 交接分支：`agent/formal-stage-gate-migration`
- 精确提交：以该 annotated tag 指向的 commit SHA 为准

## 交接范围

1. `00-opportunity` 至 `08-release-handoff`：项目各阶段文档与交付记录。
2. `workflow`：工作流状态、资产基线、外部基线输入、变更台账和入口说明。
3. `03-ui-prototype`：当前未完成的静态 HTML/CSS/JavaScript 原型。
4. `06-testing/tests`：原型静态回归检查脚本。

## 同事拉取

```bash
git clone https://github.com/QI-code1992/Equipment-repair.git
cd Equipment-repair
git fetch --tags origin
git checkout handoff/candidate-v20260714-01
```

继续开发时，请从 `agent/formal-stage-gate-migration` 或该标签创建新分支，保留该标签作为不可变交接基线。

## 原型运行

```bash
node 03-ui-prototype/prototype/local-server-4209.js
```

浏览器访问 `http://127.0.0.1:4209/pages/intelligent-config.html`。

## 交接前验证

- `06-testing/tests/*.test.js`：11/11 通过。
- `node --check`：原型 JavaScript 文件通过语法检查。
- `jq empty workflow/state.json`：状态文件格式通过检查。
- `git diff --check`：无空白错误。
- 未包含：生产接口、权限服务、安全验证、浏览器视觉验收、部署和回滚验证。

## 阶段状态

Stage 0–8 均已整理为候选资料，尚无阶段审批结论。后续接手人应先阅读 `README.md`、`workflow/state.json`、`workflow/PROJECT_ASSET_BASELINE.md` 和各阶段正式入口，再依据变更台账继续开发。
